ALGORITHM algorithm
VAR
count,i:INTEGER:=0;
n:STRING:="the nomber of characters";
BEGIN
FOR i FROM 0 TO length(n) STEP 1  DO
    IF (n[i] != " ") THEN
    count:=count+1;
END_IF
END_FOR
Write(count)
END


